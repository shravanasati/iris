# https://taskfile.dev

version: '3'


tasks:

  build:
    cmds:
      - go build -v ./cmd/iris

  run:
    cmds:
      - go run ./cmd/iris
  
  up:
    cmds:
      - go run ./cmd/iris up

  test:
    cmds:
      - go test -v ./...